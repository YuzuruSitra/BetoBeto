# モデル背景上のHUD調整（2026-09-06）

- 対象は `GameHud.cs` の配置・文字サイズ・操作バッジ。背景モデル、ステージ、ゲームルールは維持。
- Unity 6000.6.0f1 コンパイル成功、エラー0。`KitchenUiTests` 39/39成功。
- 通常描画のPlayモードでステージ1を確認。Game Viewは1920×1080、記録画像は同じ16:9の1600×900に縮小。
- `Assets/Docs/UI/hud-model-background.png`: 初期状態。カード内の材料名・数値、スコア、時間、完成率、操作説明を目視確認。
- `Assets/Docs/UI/hud-model-complete.png`: Playモード内の検証用収穫データ（13/13、スコア1,300、35秒、100%）。撮影用に `ShowComplete()` でタルトの落下を完了表示。元のステージデータへの保存はしていない。
- HUDの `TogglePause()` で一時停止と復帰の状態遷移を確認。実機パッドの通しプレイは今回の検証外。
- 独立レビューで機能回帰なし。長押しMAX文言の必要幅255に対し領域331、Controls画像の不透明部y≈794〜872で900内。
- 検証後はPlayモードを終了しTitleに復帰。

# HUD個別素材化の確認（以前の記録・2026-09-06）

ゲーム画面のHUDを9点の独立した透過PNGに分割し、背景画像を撤去しました。背景領域は今後のメッシュ実装用に残し、レシピ欄のタルトは既存の3D `TartPreview` を継続利用しています。

- Unityコンパイル：エラー0件。
- `BetoBeto.Tests.KitchenUiTests`：39/39件成功。
- HUD素材のResources読み込み、フルスクリーンアトラス不使用、外周四隅とレシピ中央穴の透過を自動確認。
- 1600×900のGame Viewを `Assets/Docs/UI/hud-individual.png` に保存し、個別HUD、実テキスト、3Dタルトを目視確認。
- 独立レビューで指摘されたステージ名サイズ、SCORE/TIME位置、透過回帰テストを修正済み。

# プロトタイプの確認記録

2026-09-05 / Windows / Unity 6000.6.0f1 / UnityCLI + Unity Pipeline

## 最新変更の確認

5種類のギミック（ゼリー、壊れるクッキー、縦/横の移動シュレッダー、4方向のスコーン、フリーザー）を追加しました。ステージは16×9・20×12のまま、内側の固定壁を減らして連動する経路を配置しています。カメラとゲームパッド操作は拡張前の仕様を維持しています。

- Unityのコンパイル：エラー0件。
- EditMode：35/35件成功。既存20件に、反射方向・クッキーの破壊/復帰/占有待ち・移動刃の反転/停止/予約マス・高速通過時の接触・旧JSONの既定値・シーン上の配置/回転・サンプル経路を追加。結果は `Logs/QA/Gimmick-EditMode-status.json`。
- Node：ステージモデル16/16件成功。新記号・移動刃を含む個数制限・旧v1互換・設定値の境界・接続性・両サンプルを確認。`editor.js` の構文チェックも成功。
- Unityシーン：Kitchenは固定刃4＋移動刃1、ゼリー2・クッキー1・スコーン2・フリーザー1。Kitchen02は固定刃6＋移動刃2、ゼリー1・クッキー3・スコーン4・フリーザー2。どちらも実際のPrefab配置から盤面情報を再構成して確認。記録は `Logs/QA/Gimmick-scenes.txt`。
- ギミックの見た目はPrefab、マテリアル、三角メッシュを追加。既存の床、カメラ、照明と保持できる配置物はそのまま使用しています。
- 静止画：EditModeのキッチンカメラから盤面を描画し、丸いクッキー・三角の斜面・ゼリー・雪印の床・車輪付きの刃を確認。`Logs/QA/Gimmicks-static-layout.png`。ゲーム進行やパッド操作は行っていません。

実機パッドでの動作、連鎖の気持ちよさ、移動刃と凍結のタイミングの確認はユーザーが行う方針です。今回、PlayModeテストやブラウザのゲーム操作は実行していません。既存のPlayModeテストは、新しい配置に依存しない初期化とクッキーの通過対象に更新しました。EditModeはルール・状態の検証であり、実プレイ確認を代替するものではありません。

最終調整で、フリーザー上へ巻き込まれて滑走開始する場合にも凍結を適用しています。調整後のコンパイルもエラー0件です。

WebGLの最終ビルドは成功（15,930,971 bytes、155.6秒、エラー0件）。出力は `Builds/WebGL`、結果は `Logs/QA/Gimmick-WebGL-build-report.json`。警告1件はUnity Pipelineの配布用Playerサーバーが無効になる通知です。ゲーム本体のコンパイルエラーではありません。

## 驚かすの変更（前回の確認）

単押しの対象を自分のマスと前方1マスに広げ、対象の敵がプレイヤーの向いている方向へ進むように変更しました。驚かせた瞬間に進行先の隣接1マスがシュレッダーなら、単押し・長押しとも、よだれなしで直接突入します。通常歩行中は引き続き刃を避けます。

最新コードのコンパイルはエラー0件。動作確認はユーザーが実施し、2026-09-05にOKの報告を受けました。この変更に合わせたPlayModeテストの定義も更新していますが、最終仕様での自動テストとWebGL再ビルドは実施していません。

## ゲームの確認範囲（上記変更前の記録）

以下は、氷生成を「驚かす」に置き換え、よだれの再使用待ちを0.3秒・驚かすの再使用待ちをなしにした段階の記録です。EditMode 20/20件成功、PlayMode 39/39件成功。生データは `Logs/QA/Scare-EditMode-results.xml`、`Logs/QA/Scare-PlayMode-results.xml`、`Logs/QA/Scare-PlayMode-status.json` に保存しています。

- EditMode：JSON検証、座標変換、レシピの勝敗条件、脱出上限、フルーツの進行ルール、Prefab参照。
- PlayMode：タイトル→ステージ選択→2番目のステージ→勝利リザルト→再挑戦、敗北リザルト→ステージ選択。各画面が別シーンで、音声オブジェクトが重複しないこと。
- よだれの配置制限・寿命・ポーズ中の停止、よだれを直接踏ませた滑走開始、3体の巻き込み・共有加速・900点の収穫、壁での停止と反動、メロンの耐久と無敵時間。
- 4種類とも通常移動中はシュレッダーを避けること。壁と刃に囲まれると待機し、よだれを置いた場合にだけ刃に突入すること。
- 両サンプルステージで4種類それぞれの移動を60秒分進め、よだれなしでは収穫数が増えないこと。
- 仮想ゲームパッドからの実入力で、ゴーストがクッキー壁を通過できること。テスト中だけ入力のフォーカス条件を固定し、終了時に戻す。
- 右スティックで位置を変えずに向きが変わり、スティックの小さな揺れで向きが変わらないこと。下ボタンで足元によだれ、左ボタンの単押しで前方1マスだけの敵が逃げること。空振りで背後の敵が影響を受けないこと。
- 0.25秒の単押し／チャージ境界、途中チャージの範囲、1.5秒での上限、最大時の半径6マスの境界を確認。円の外側・7マス先は影響を受けず、左右の敵がそれぞれ外向きに変わること。
- 実際のInput Systemイベントで、押している間は発動せず、長押しが最大6マスで止まり、離すと周囲の敵に効果が出ること。ポーズ・切断によってチャージが取り消され、再開時に遅れて発動しないこと。
- 4種類とも驚かされた直後に通常AIで向きを戻さず逃げ、マスの途中でも座標が飛ばずに反転すること。当時の仕様で、逃げる先が刃でほかに安全な道がなければ待ち、よだれがある場合だけ滑って収穫されること。
- プレイヤーと敵が同じマスの中にいる場合も、マスの中心ではなくプレイヤーの実際の立ち位置から離れる方向を選ぶこと。
- 驚かす→よだれに進入→別の敵を巻き込む→2体を収穫、という組み合わせ。既に滑っている敵を驚かしても共有の連鎖数が維持されること。
- よだれの待ち時間が0.3秒で、0.4秒後に次のマスへ再使用できること。驚かすを同じフレーム内に再使用でき、パッドでも連続した単押しや直後の再チャージができること。
- フルーツの顔が通常移動と滑走方向に一致すること。
- パッド入力だけでタイトル→選択→2番目のステージ→音量変更→復帰→リザルト→再挑戦→選択→タイトルと遷移できること。決定／復帰の入力が本編のよだれ設置に漏れないこと。
- デバイスを無効化すると一時停止し、再接続後も復帰操作まで停止を保つこと。実機パッドが接続されている場合も検証できるよう、テスト実行中だけ既存パッドのUnity入力を無効化し、終了時に元の状態へ戻します。

## 画面確認（上記変更前の記録）

Unity Game Viewを1920×1080でキャプチャして、タイトル、ステージ選択、ゲーム画面、一時停止・音量設定、リザルトを確認しました。連鎖と壁の演出は、通常のステージ内にフルーツを配置した確認用の場面を使っています。タイトルから選択を経由する本番の起動経路はPlayModeテストで別途確認しています。

同じシュレッダーで3体を連続収穫した際、HUDの得点が900、収穫数が3、盤面の得点表示が「+900」になることを確認しました。演出を含む実行中、Consoleのエラーは0件でした。

画像とテストの生データは `Logs/QA/` に保存しています。このフォルダは既存の `.gitignore` によりGit管理対象外です。

驚かす機能の画面確認では、通常ステージ上の確認用配置で最大チャージ、発動時の波紋と「！」、更新した一時停止画面を撮影しました。紫色の範囲円、白いチャージ円、HUDのゲージ・半径・離す操作の案内を確認しています。画像は `Logs/QA/Screenshots/Scare-*.png`。確認後は仮想パッドを除去し、既存パッドと入力設定を元に戻しています。

## 独立ステージエディタ（ギミック追加前の記録）

Nodeでモデルのテスト10件を確認。JSONの入出力、配置制約、パイプからの到達可能性、リサイズ、Undo、Unity用サンプルとの一致を検証しています。JavaScriptの構文チェックも通っています。

ブラウザツールがローカルHTMLへのアクセスを許可しなかったため、ブラウザ内でのマウス操作・表示の確認は行っていません。`Tools/StageEditor/index.html` をローカルのChromeまたはEdgeで開いて利用する構成です。

## Windowsビルド（ゲームパッド対応前の記録）

`Builds/Windows/BetoBeto.exe` のStandaloneWindows64ビルドに成功しました。出力合計は約118 MB、エラー0件です。ビルドレポートは `Logs/QA/Windows-build-report.json` に保存しています。

最終ビルドの警告1件は、配布用PlayerにRuntimePipelineConfigがないためUnity Pipelineの実行時サーバーが無効になるという通知です。Unity EditorからのUnityCLI操作は利用できます。

生成した実行ファイルをウィンドウなしで5秒間起動し、起動中の例外・エラーログがないことを確認して終了しました（`Logs/QA/Windows-startup.log`）。描画と操作の確認はUnity Editorで行っています。

保存・ビルドに伴い、既存のURPと描画設定もUnity 6000.6のシリアライズ形式へ更新されています。

## 現段階の素材

モデルと音は差し替え可能なプロトタイプ素材です。フルーツ・壁・床・罠・配置物はPrefab。日本語表示にはM PLUS Rounded 1cを同梱しています。ステージ難易度と長時間プレイ時のバランス調整は今後の作業になります。

## WebGLとゲームパッド対応

主要ターゲットをWebGLに切り替えました。出力は `Builds/WebGL`、再ビルドは `BetoBeto → Build → WebGL`、ローカル起動は `python Tools/serve_webgl.py` です。Web向けの初期メモリ128MB・最大1024MB、Gzipと展開フォールバック、日本語フォント、クリックによる音声開始画面を設定しています。

ブラウザでタイトル→ステージ選択→キッチン→一時停止画面を確認しました。Web版のパッド未接続時は、開始前で進行が止まり、接続案内が表示されます。日本語の説明と音量スライダーに重なりはありません。画面拡大時の文字のぼやけを防ぐため、Canvasの倍率が変わると文字メッシュを再生成します。

パッドの移動、方向転換、よだれ、驚かす単押し／長押し、メニュー操作はUnityの仮想Gamepadを使う自動テストで確認しています。ブラウザ内の実機パッドでの操作と、実際の音の聴感確認は未実施です。

単押しの対象拡張・直接突入の追加前のWebGLビルドは成功（15,905,465 bytes、エラー0件、警告1件）。驚かすのチャージ、よだれ0.3秒・驚かす待ち時間なしを含むレポートは `Logs/QA/Scare-WebGL-build-report.json` に保存しています。HTTP経由でタイトル→ステージ選択→キッチンを確認し、このビルドへ再読み込みした後もタイトルが起動し、ブラウザのエラーログは0件でした。

ゲームパッド対応時には385×800で起動してから1280×800へ拡大し、タイトルの日本語とボタンの文字が鮮明に描き直されることを確認しています。今回はブラウザの画面サイズを変更していません。

今回のビルド警告1件は、Unity PipelineのPlayerサーバー無効化に関する通知です。Unity EditorからのUnityCLI操作は利用できます。ブラウザではFSR補間シェーダーの未対応／除外について警告が出ていますが、実行時エラーはありませんでした。現在のゲームはポストプロセス効果を使用していません。

Webの入力仕様は[Unity公式マニュアル](https://docs.unity3d.com/6000.0/Documentation/Manual/webgl-input.html)、音声開始の制約は[Web Audioの公式説明](https://docs.unity3d.com/6000.0/Documentation/Manual/webgl-audio.html)に沿っています。

## 既存ステージのJSON更新（2026-09-05）

`Stage JSON Importer` に「既存シーンを更新して保存」を追加し、ユーザーが本機能を使用してステージ1を更新しました。今後の動作確認はユーザーが行います。この変更のために追加した自動テストと既存テストへの調整は、ユーザーの指定により取り除いています。
